# Deployer wallet private key (the organizer's wallet)
# Get a private key from MetaMask or generate one with: node scripts/generate-wallets.js
DEPLOYER_PRIVATE_KEY=

# Monad testnet RPC (default works, but you can use a custom one for higher rate limits)
MONAD_RPC_URL=https://testnet-rpc.monad.xyz
